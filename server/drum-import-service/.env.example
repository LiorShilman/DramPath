# Copy to .env and fill in your own key to enable the optional Fadr-backed
# "upload a full song" import path (ADR 0008). Leave unset/absent to keep
# the service exactly as it was before — the manual stem-upload endpoint
# (/api/v1/analyze) never depends on this.
FADR_API_KEY=
